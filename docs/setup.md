# Setup

1. Install Rust toolchain (`rustup`, stable channel).
2. Add local model assets under `models/`.
3. Choose a hardware profile from `configs/`.
4. Start orchestrator:

```bash
cargo run --manifest-path orchestrator/Cargo.toml
```
